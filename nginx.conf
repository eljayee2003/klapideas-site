server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ =404;
  }

  location /assets/ {
    expires 7d;
  }

  # (Reserved for later) reverse proxy to KilimaHUB under /hub/
  # location /hub/ {
  #   proxy_pass http://kilimahub:8000/;
  #   proxy_set_header Host $host;
  #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #   proxy_set_header X-Forwarded-Proto $scheme;
  # }
}
